name: pp
defaults:
  stage: dev
  environment: dev
  solution: Core
solutions:
  Core:
    environment: dev
    uniqueName: Core
assets:
  apps: apps
  flows: flows
  solutions: solutions
  docs: docs
providerBindings:
  primaryDataverse:
    kind: dataverse
    target: dev
    description: Primary Dataverse environment alias for this project
topology:
  defaultStage: dev
  stages:
    dev:
      environment: dev
      solution: Core
docs:
  paths:
    - docs
